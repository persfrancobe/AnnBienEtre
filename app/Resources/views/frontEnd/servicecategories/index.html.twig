{% extends "frontEnd/listing-layout.html.twig" %}
{% block titre %} Tout Les Categories
{% endblock %}
{% block posts %}

    {% if  service_categories is iterable%}
        {%for category in  service_categories%}
            {{ include('Partials/big-post.html.twig',{post:{id:category.id, name:category.name,image:'img/logo.png',description:category.description,rout:'servicecategory_show'}}) }}
        {%endfor%}
    {%else%}
        {{ include('Partials/big-post.html.twig',{post:{id:category.id, name:category.name,image:'img/logo.png',description:category.description,rout:'servicecategory_show'}}) }}
    {%endif%}
{%endblock%}