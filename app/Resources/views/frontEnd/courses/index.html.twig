{% extends "frontEnd/listing-layout.html.twig" %}
{% block titre %}Tout Les courses
{% endblock %}
{% block posts %}

    {% if  courses is iterable%}
        {%for course in  courses%}
            {{ include('Partials/big-post.html.twig',{post:{id:course.id, nom:course.name,image:'/Resources/public/img/logo.png',description:course.description,rout:'courses_show'}}) }}
        {%endfor%}
    {%else%}
        {{ include('Partials/big-post.html.twig',{post:{id:course.id, nom:course.name,image:'/Resources/public/img/logo.png',description:course.description,rout:'courses_show'}}) }}
    {%endif%}
{%endblock%}