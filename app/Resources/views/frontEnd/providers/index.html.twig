{% extends "frontEnd/listing-layout.html.twig" %}
{% block titre %}Prestataire
{% endblock %}
{% block h1 %}Nos Prestataires{% endblock %}
{% block h2 %}{{ providers_result|length }}  Prestataire trouvé a votre recherche{% endblock %}
{% block posts %}

    {% if  providers_result is iterable %}
        {% for provider in  providers_result %}
            {{ include('Partials/big-post.html.twig',{post:{id:provider.id,name:provider.username,image:provider.images,description:provider.email,rout:'provider_show'}}) }}
        {% endfor %}

    {% else %}
        {{ include('Partials/big-post.html.twig',{post:{id:providers_result.id,name:providers_result.name,image:provider.images,description:providers_result.email,rout:'provider_show'}}) }}
    {%endif%}

{%endblock%}
{% block pager %}{{ knp_pagination_render(providers_result) }}{% endblock %}
{% block side %}  {{ render(controller('AppBundle:FrontEnd/Default:sidebar')) }}{% endblock %}